#!/usr/bin/env ruby

aws_module = ARGV[0] || nil

case aws_module
when 'instances'
  require_relative '../lib/instances'
when 'ssm'
  require_relative '../lib/ssm'
when 'vpc'
  require_relative '../lib/vpc'
else
  puts 'Module not found. Currently supported modules are: instances, ssm, vpc'
end
